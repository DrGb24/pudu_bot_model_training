{
  "config": {
    "sequence_length": 10,
    "lstm_units": 128,
    "dropout_rate": 0.3,
    "dense_units": 64,
    "learning_rate": 0.0001,
    "batch_size": 16,
    "epochs": 100,
    "early_stopping_patience": 15,
    "future_window": 168
  },
  "input_shape": [
    10,
    9
  ],
  "output_names": [
    "is_failure_now",
    "severity_class",
    "future_failure_prob",
    "hours_to_failure_norm"
  ]
}